# rule-workbench

Terminal tooling for rule maintainers: inspect / test / diff / scaffold / sanitize / promote
commands over the declarative ruleset (see `spec/500-testing/RuleMaintainerWorkflow.md`).

Introduced in **M8 — Export and maintainer workflow**. Placeholder in M0 to match the monorepo
layout in `spec/200-architecture/System.md`.
